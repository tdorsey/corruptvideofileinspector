{
  "name": "corruptvideofileinspector-monorepo",
  "version": "1.0.0",
  "description": "Corrupt Video Inspector monorepo with Nx",
  "private": true,
  "scripts": {
    "test": "nx run-many --target=test --all",
    "lint": "nx run-many --target=lint --all",
    "build": "nx run-many --target=build --all",
    "format": "nx run-many --target=format --all",
    "graph": "nx graph",
    "web:dev": "nx serve web",
    "web:build": "nx build web",
    "web:test": "nx test web",
    "web:lint": "nx lint web"
  },
  "devDependencies": {
    "nx": "^22.3.3",
    "@aws/nx-plugin": "^0.65.0",
    "@nx/js": "^22.3.3",
    "typescript": "^5.2.2",
    "eslint": "^8.53.0",
    "prettier": "^3.1.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "packageManager": "pnpm@10.28.0",
  "nx": {
    "includedScripts": []
  }
}
