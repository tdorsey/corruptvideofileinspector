# Development Dockerfile - uses the development stage from main Dockerfile
FROM . AS development

# This Dockerfile simply targets the development stage from the main Dockerfile
# All development-specific setup is handled in the multi-stage build

# The development stage includes:
# - All base dependencies (Python, FFmpeg, dumb-init)
# - Development tools (git, curl, vim, nano, zsh, build-essential)
# - Python dependencies installed in user space
# - Development environment variables
# - Container kept alive with tail -f /dev/null
