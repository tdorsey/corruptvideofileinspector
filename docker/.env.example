# Docker Compose Environment Variables Example
# Copy this file to .env and update with your actual values

# Application Version
APP_VERSION=1.0.0

# Video Directory Paths (required for bind mounts)
CVI_VIDEO_DIR=/path/to/your/videos
CVI_OUTPUT_DIR=/path/to/output
CVI_LOG_DIR=/path/to/logs

# Trakt.tv Configuration (for trakt service)
# Note: Client credentials are also available via Docker secrets in secrets/ directory
TRAKT_CLIENT_ID=your_trakt_api_client_id_here

# Usage Examples:
#
# Run scan only:
#   docker-compose up scan
#
# Run scan and report:
#   docker-compose up scan report
#
# Run scan and sync to Trakt (with profile):
#   docker-compose --profile trakt up scan trakt
#
# Development mode with Trakt:
#   docker-compose -f docker-compose.dev.yml --profile trakt up app trakt-dev
