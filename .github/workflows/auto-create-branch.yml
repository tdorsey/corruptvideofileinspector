name: 🌿 Auto Create Branch from Issue

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  create-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create branch and comment on issue
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = context.issue.number;
            const issueTitle = context.payload.issue.title;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const commentMarker = '<!-- auto-branch-comment -->';
            
            // Create a slug from the issue title
            const slug = issueTitle
              .toLowerCase()
              .replace(/[^a-z0-9\s-]/g, '')
              .replace(/\s+/g, '-')
              .replace(/-+/g, '-')
              .replace(/^-|-$/g, '')
              .substring(0, 50);

            const branchName = `issue-${issueNumber}-${slug}`;
            
            const minimizeComment = async (nodeId) => {
              if (!nodeId) {
                return;
              }
              try {
                await github.graphql(
                  `mutation($subjectId:ID!, $classifier:ReportedContentClassifiers!) {
                    minimizeComment(input:{subjectId:$subjectId, classifier:$classifier}) {
                      minimizedComment { isMinimized }
                    }
                  }`,
                  { subjectId: nodeId, classifier: 'OUTDATED' }
                );
              } catch (error) {
                console.log('Unable to minimize branch comment:', error.message);
              }
            };
            
            const { data: comments } = await github.rest.issues.listComments({
              owner,
              repo,
              issue_number: issueNumber,
              per_page: 100
            });
            
            const existingComment = comments.find(comment =>
              comment.body &&
              (comment.body.includes(commentMarker) ||
                comment.body.includes('🌿 **Branch Created**') ||
                comment.body.includes('⚠️ **Branch Creation Failed**'))
            );
            
            try {
              // Get the default branch reference
              const { data: defaultBranch } = await github.rest.repos.get({
                owner,
                repo
              });
              
              // Get the SHA of the default branch
              const { data: ref } = await github.rest.git.getRef({
                owner,
                repo,
                ref: `heads/${defaultBranch.default_branch}`
              });
              
              // Create the new branch
              await github.rest.git.createRef({
                owner,
                repo,
                ref: `refs/heads/${branchName}`,
                sha: ref.object.sha
              });
              
              console.log(`✅ Branch created successfully: ${branchName}`);
              
              // Post a success comment on the issue
              const commentBody = [
                commentMarker,
                `🌿 **Branch Created**: \`${branchName}\``,
                '',
                'Thank you for opening this issue! A branch has been automatically created for you to work on this feature/fix.',
                '',
                '## Getting Started',
                `1. **Fetch the branch**: \`git fetch origin ${branchName}\``,
                `2. **Switch to the branch**: \`git checkout ${branchName}\``,
                '3. **Start development**: Make your changes on this branch',
                `4. **Create a Pull Request**: When ready, open a PR from \`${branchName}\` to \`main\``,
                '',
                '## Development Guidelines',
                `- Follow the [development instructions](https://github.com/${context.repo.owner}/${context.repo.repo}/blob/main/.github/copilot-instructions.md)`,
                '- Run `make check` before committing to ensure code quality',
                '- Run `make test` to validate your changes',
                '- Use conventional commit messages (e.g., `feat:`, `fix:`, `docs:`)',
                '',
                'Happy coding! 🚀'
              ].join('\n');

              if (existingComment) {
                await minimizeComment(existingComment.node_id);
              } else {
                const { data: branchComment } = await github.rest.issues.createComment({
                  issue_number: issueNumber,
                  owner,
                  repo,
                  body: commentBody
                });
                
                await minimizeComment(branchComment.node_id);
              }
              
              console.log(`✅ Comment posted successfully on issue #${issueNumber}`);
              
            } catch (error) {
              console.error('❌ Error creating branch or posting comment:', error.message);
              
              // If branch creation fails, post a comment about the failure
              const failureComment = [
                commentMarker,
                '⚠️ **Branch Creation Failed**',
                '',
                'There was an issue creating an automatic branch for this issue. This might be because:',
                '- A branch with a similar name already exists',
                '- There was a temporary GitHub API issue',
                '',
                'You can manually create a branch using:',
                '```bash',
                `git checkout -b issue-${issueNumber}-${slug}`,
                '```',
                '',
                `The suggested branch name would have been: \`issue-${issueNumber}-${slug}\``
              ].join('\n');

              try {
                if (existingComment) {
                  await minimizeComment(existingComment.node_id);
                } else {
                  const { data: failureCommentData } = await github.rest.issues.createComment({
                    issue_number: issueNumber,
                    owner,
                    repo,
                    body: failureComment
                  });
                  
                  await minimizeComment(failureCommentData.node_id);
                }
              } catch (commentError) {
                console.error('❌ Error posting failure comment:', commentError.message);
              }
            }
